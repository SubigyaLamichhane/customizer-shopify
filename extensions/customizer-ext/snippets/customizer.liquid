<script>
  const APP_URL = "https://panel-print-connecticut-coverage.trycloudflare.com";
  const SHOP_URL = "offline_" + Shopify.shop;
  var tempCurrency = "{{ shop.money_format }}";
  const CURRENCY = tempCurrency.split('{')[0];
  const SHOP_THEME = Shopify.theme.name;
  const PAGE_HANDLE = "{{ page.handle }}";
  const HANDLE = "{{ template | handle }}";
  const PRODUCT = {{ product | json }};
</script>